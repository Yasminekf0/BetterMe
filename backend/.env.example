# Master Trainer Backend Environment Variables
# 主训练师后端环境变量配置

# ==================== Server Configuration / 服务器配置 ====================
PORT=3001
NODE_ENV=development

# ==================== Database Configuration (MySQL) / 数据库配置 ====================
# Format: mysql://username:password@host:port/database
# 格式: mysql://用户名:密码@主机:端口/数据库名
DATABASE_URL="mysql://betterme:betterme@47.82.66.146:3306/betterme"

# ==================== JWT Configuration / JWT配置 ====================
JWT_SECRET=master-trainer-jwt-secret-2026-secure-key
JWT_EXPIRES_IN=7d

# ==================== AI API Configuration (Aliyun Bailian) / AI API配置(阿里云百炼) ====================
# 重要: AI模型配置（包括API Key和API地址）存储在MySQL ai_models表中，通过数据库查询获取
# Important: AI model configs (including API Key and endpoints) stored in MySQL ai_models table, retrieved via DB query
#
# API文档 / API Documentation: https://help.aliyun.com/zh/model-studio/
#
# 数据库中各模型的真实配置 / Real configs for each model in database:
# ┌─────────────────────────┬──────────────────────────────────────────────────────────────────────────┐
# │ Model (模型)            │ API Endpoint (接口地址)                                                    │
# ├─────────────────────────┼──────────────────────────────────────────────────────────────────────────┤
# │ qwen3-max (CHAT)        │ https://dashscope.aliyuncs.com/compatible-mode/v1                        │
# │ text-embedding-v2       │ https://dashscope.aliyuncs.com/compatible-mode/v1                        │
# │ qwen2.5-vl-embedding    │ https://dashscope.aliyuncs.com/api/v1/services/embeddings/multimodal-embedding/multimodal-embedding │
# │ qwen3-tts-flash (TTS)   │ https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation │
# │ qwen3-asr-flash (STT)   │ wss://dashscope.aliyuncs.com/api-ws/v1/realtime                          │
# └─────────────────────────┴──────────────────────────────────────────────────────────────────────────┘
#
# 所有模型共用的API Key (存储在数据库ai_models表中)
# Shared API Key for all models (stored in ai_models table):
# sk-52389248f75444f4bf0f37f5afff799d

# 请求设置（备用配置）/ Request Settings (fallback config)
AI_TIMEOUT=30000
AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.7

# ==================== Frontend URL (for CORS) / 前端URL ====================
FRONTEND_URL=http://localhost:3000

# ==================== Session Configuration / 会话配置 ====================
SESSION_SECRET=master-trainer-session-secret-2026-secure-key

# ==================== Aliyun OSS Storage Configuration / 阿里云OSS存储配置 ====================
# 真实配置（从数据库storage_configs表获取）
# Real configuration (from storage_configs table in database)
# Documentation: https://help.aliyun.com/document_detail/31886.html

# OSS Access Key ID
OSS_ACCESS_KEY_ID=your_oss_access_key_id_here

# OSS Access Key Secret
OSS_ACCESS_KEY_SECRET=your_oss_access_key_secret_here

# OSS Region / OSS区域
OSS_REGION=oss-cn-hongkong

# OSS Bucket Name / OSS存储桶名称
OSS_BUCKET=beterrme-audiofiles

# OSS Endpoint / OSS端点
OSS_ENDPOINT=oss-cn-hongkong.aliyuncs.com

# OSS Base URL - CDN or custom domain (optional)
# OSS基础URL - CDN或自定义域名(可选)
OSS_BASE_URL=
